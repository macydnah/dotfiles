Symlink here the configuration files in pipewire-pulse.conf.avail and pipewire.conf.avail to enable them. You can also copy them here if you want to edit them. For example:

```bash
ln -s ~/.config/pipewire.conf.avail/10-rates.conf ~/.config/pipewire.conf.d/10-rates.conf
```
